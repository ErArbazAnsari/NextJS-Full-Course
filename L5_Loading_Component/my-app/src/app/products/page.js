import React from "react";

const dashboard = ({ searchParams }) => {
    console.log(searchParams);
    return (
        <div>
            <h1>Product Page</h1>
        </div>
    );
};

export default dashboard;
