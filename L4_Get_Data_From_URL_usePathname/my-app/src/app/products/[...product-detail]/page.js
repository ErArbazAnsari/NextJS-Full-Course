import React from "react";

const page = ({params}) => {
    console.log(params);
    return (
        <div>
            <h1>hello from arbaz, i catch all slug all the segment/route.</h1>
        </div>
    );
};

export default page;
